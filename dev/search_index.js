var documenterSearchIndex = {"docs":
[{"category":"section","location":"#Frostlake.jl","page":"Home","text":"A Julia client for Frostlake, a SQL engine that emulates Snowflake. It talks to a running Frostlake server (DatabaseHttpServer) over its HTTP API and depends only on Julia's standard library: Downloads for the transport and Dates for temporal types.","title":"Frostlake.jl"},{"category":"section","location":"#Requirements","page":"Home","text":"Julia 1.10 or newer.\nA Frostlake engine 0.0.7 or newer. SELECT CURRENT_VERSION() reports a server's version. The driver speaks the HTTP protocol rather than linking the engine, so this is a minimum, not a pinned version.","title":"Requirements"},{"category":"section","location":"#Installation","page":"Home","text":"pkg> add Frostlake","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Frostlake\n\nconn = Connection(\"frostlake://localhost:18082/MY_DB?schema=PUBLIC\")\n\nexecute(conn, \"CREATE TABLE people (id INTEGER, name VARCHAR)\")\n\ninserted = execute(conn, \"INSERT INTO people VALUES (?, ?), (?, ?)\",\n                   [1, \"Ada\", 2, \"Grace\"])\ninserted.updatecount            # 2\n\nresult = execute(conn, \"SELECT id, name FROM people WHERE id = ?\", [1])\nrows(result)[1][\"NAME\"]         # \"Ada\"\n\nclose(conn)","title":"Quick start"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"connecting.md\", \"statements.md\", \"results.md\", \"transactions.md\", \"errors.md\",\n         \"limitations.md\", \"development.md\", \"api.md\"]\nDepth = 1","title":"Contents"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Connecting","page":"API reference","text":"","title":"Connecting"},{"category":"section","location":"api/#Statements","page":"API reference","text":"","title":"Statements"},{"category":"section","location":"api/#Transactions","page":"API reference","text":"","title":"Transactions"},{"category":"section","location":"api/#Results","page":"API reference","text":"","title":"Results"},{"category":"section","location":"api/#Values","page":"API reference","text":"","title":"Values"},{"category":"section","location":"api/#Errors","page":"API reference","text":"","title":"Errors"},{"category":"module","location":"api/#Frostlake","page":"API reference","text":"Frostlake\n\nA driver for Frostlake, speaking the engine's HTTP protocol against a running DatabaseHttpServer.\n\nusing Frostlake\n\nconn = Connection(\"frostlake://localhost:18082/MY_DB?schema=PUBLIC\")\nexecute(conn, \"CREATE TABLE people (id INTEGER, name VARCHAR)\")\nexecute(conn, \"INSERT INTO people VALUES (?, ?)\", [1, \"Ada\"])\nresult = execute(conn, \"SELECT name FROM people WHERE id = ?\", [1])\nscalar(result)  # \"Ada\"\nclose(conn)\n\nNothing outside the standard library is needed: the JSON reader, the DSN parser and the SQL scanner are all in the package, and the transport is Downloads.\n\n\n\n\n\n","title":"Frostlake"},{"category":"type","location":"api/#Frostlake.Connection","page":"API reference","text":"Connection\n\nA connection to a Frostlake HTTP server, and the engine session behind it.\n\nStatements are serialized: a statement holds the connection until it has an answer, so session state — USE, session variables, an open transaction — carries from one statement to the next, which is exactly what a half-interleaved second statement would break.\n\nBuild one with Connection(dsn) and release it with close.\n\n\n\n\n\n","title":"Frostlake.Connection"},{"category":"function","location":"api/#Frostlake.connect","page":"API reference","text":"Connection(dsn; kwargs...) -> Connection\nConnection(f, dsn; kwargs...)\n\nOpens a connection to a Frostlake HTTP server. Frostlake.connect is the same function under the name other drivers use; it is not exported, because connect is Sockets' name too.\n\nconn = Connection(\"frostlake://localhost:18082/MY_DB?schema=PUBLIC\")\n\nThe server is contacted before this returns: its health endpoint is called, and the database, schema, role and warehouse the DSN names are selected. A name that does not exist is therefore reported here, rather than surfacing later on whichever query happened to run first.\n\nGiven a function, the connection is passed to it and closed afterwards:\n\nConnection(\"frostlake://localhost:18082\") do conn\n    execute(conn, \"SELECT 1\")\nend\n\nEvery keyword may also be given in the DSN query string, where an explicit keyword outranks it. Timeouts are seconds, or a fixed-length Dates.Period such as Minute(10); 0 removes the bound.\n\nKeyword Meaning\ntimeout how long one statement may take\nconnect_timeout how long to wait for the socket\nidle_limit how long a connection may idle before its scope is re-applied\ncacert a CA bundle for HTTPS, instead of the system's\nverify_certificate false to accept any HTTPS certificate\n\n\n\n\n\n","title":"Frostlake.connect"},{"category":"function","location":"api/#Frostlake.ping","page":"API reference","text":"ping(conn)\n\nChecks that a Frostlake engine is answering, via GET /api/health.\n\nA 200 on its own only says something is listening — anything can serve that. The health payload is what says it is an engine, so a body that is not one is reported rather than passed off as healthy.\n\n\n\n\n\n","title":"Frostlake.ping"},{"category":"function","location":"api/#Frostlake.apply_dsn_scope","page":"API reference","text":"apply_dsn_scope(conn)\n\nSelects the database, schema, role and warehouse the DSN names. connect calls this, so it is only worth calling again after the session has been moved somewhere else deliberately.\n\n\n\n\n\n","title":"Frostlake.apply_dsn_scope"},{"category":"function","location":"api/#Frostlake.session_id","page":"API reference","text":"The engine's id for this connection's session, once it has one.\n\n\n\n\n\n","title":"Frostlake.session_id"},{"category":"function","location":"api/#Frostlake.in_transaction","page":"API reference","text":"Whether a transaction is open — begin_transaction without a matching commit or rollback.\n\n\n\n\n\n","title":"Frostlake.in_transaction"},{"category":"function","location":"api/#Frostlake.base_url","page":"API reference","text":"The base URL of the server, without a trailing slash.\n\n\n\n\n\nThe server this connection speaks to, as scheme://host:port.\n\n\n\n\n\n","title":"Frostlake.base_url"},{"category":"function","location":"api/#Frostlake.execute","page":"API reference","text":"execute(conn, sql; multi_statement_count=nothing) -> Result\nexecute(conn, sql, parameters; multi_statement_count=nothing) -> Result\n\nRuns one statement and returns its first result set.\n\nparameters fill the statement's placeholders: a vector or tuple for positional ? markers, a dictionary or named tuple for :name markers. The count has to match exactly whenever arguments are supplied; with none at all the markers pass through to the server, where Snowflake Scripting binds them (OPEN c USING (...), EXECUTE IMMEDIATE :v).\n\nexecute(conn, \"INSERT INTO people VALUES (?, ?)\", [1, \"Ada\"])\nexecute(conn, \"SELECT :a + :b AS total\", (a=2, b=40))\n\nmulti_statement_count says how many statements this one request carries; see execute_all.\n\nA string holding several ;-separated statements answers with the first one's result — use execute_all for the rest.\n\n\n\n\n\n","title":"Frostlake.execute"},{"category":"function","location":"api/#Frostlake.execute_all","page":"API reference","text":"execute_all(conn, sql; multi_statement_count=nothing) -> Vector{Result}\nexecute_all(conn, sql, parameters; multi_statement_count=nothing) -> Vector{Result}\n\nRuns a statement string and returns every result set it produced, in order. A single statement gives a one-element vector.\n\nThe engine refuses a request holding more statements than it was told to expect, so a pack says how many it holds:\n\nexecute_all(conn, \"SELECT 1; SELECT 2\"; multi_statement_count=2)\n\nThe count travels with this one request. It outranks the session's MULTI_STATEMENT_COUNT without changing it, so there is nothing to put back afterwards, and 0 allows any number. Left out, no count is sent at all and the session's value decides.\n\n\n\n\n\n","title":"Frostlake.execute_all"},{"category":"function","location":"api/#Frostlake.transaction","page":"API reference","text":"transaction(f, conn)\n\nRuns f(conn) inside BEGIN ... COMMIT, rolling back if it throws and re-raising the original error either way.\n\ntransaction(conn) do c\n    execute(c, \"INSERT INTO acc VALUES (1)\")\nend\n\nThe connection is not held for the duration: a transaction lives on the session, so anything else run on this same connection meanwhile joins the transaction. Give a transaction its own connection if that is not what you want.\n\n\n\n\n\n","title":"Frostlake.transaction"},{"category":"function","location":"api/#Frostlake.begin_transaction","page":"API reference","text":"begin_transaction(conn)\n\nOpens a transaction: autocommit goes off and BEGIN is sent.\n\n\n\n\n\n","title":"Frostlake.begin_transaction"},{"category":"function","location":"api/#Frostlake.commit","page":"API reference","text":"Commits the open transaction and restores autocommit.\n\n\n\n\n\n","title":"Frostlake.commit"},{"category":"function","location":"api/#Frostlake.rollback","page":"API reference","text":"Rolls the open transaction back and restores autocommit.\n\n\n\n\n\n","title":"Frostlake.rollback"},{"category":"type","location":"api/#Frostlake.Result","page":"API reference","text":"Result\n\nOne result set: what a single statement answered with.\n\nField What it holds\ncolumns a ColumnInfo per column, in order\nvalues every cell, positionally aligned with columns — the lossless view\nupdatecount rows affected by DML, or -1 when the statement returned data\ncounters the raw number of rows ... counters behind updatecount\n\nA Result iterates and indexes over its rows, so for row in result and result[1][2] work directly, and rows gives the same rows keyed by column name.\n\nA DML statement answers with a status grid rather than data — one row of number of rows inserted-style counters — and that grid is reported here as it arrived. updatecount is the friendly reading of it.\n\n\n\n\n\n","title":"Frostlake.Result"},{"category":"type","location":"api/#Frostlake.ColumnInfo","page":"API reference","text":"ColumnInfo\n\nWhat the engine reported about one column of a result set: its name as the engine cased it, the declared datatype (NUMBER, VARCHAR, TIMESTAMP_NTZ, ...), whether it is nullable, the precision and scale of the fixed-point numerics, and the length of the text and binary ones.\n\nlength counts characters for a VARCHAR and bytes for a BINARY, and is what the account's own driver reports as such a column's precision and its display size: VARCHAR(9) is 9. A table column declared without a width carries 16777216 characters or 8388608 bytes; a cast without one, such as 'abc'::VARCHAR, reports 134217728 characters or 67108864 bytes, and a string literal its own length. Only those two families carry it; every other type leaves it nothing.\n\nnullable, precision, scale and length are nothing when the server did not say — an engine that predates a field reports nothing rather than false or 0.\n\n\n\n\n\n","title":"Frostlake.ColumnInfo"},{"category":"function","location":"api/#Frostlake.rows","page":"API reference","text":"rows(result) -> Vector{Dict{String,Any}}\n\nEach row keyed by column name, built on first use and kept.\n\nA dictionary cannot represent two columns called the same thing — a self-join reports ID twice and the later one wins — so result.values stays the lossless view.\n\n\n\n\n\n","title":"Frostlake.rows"},{"category":"function","location":"api/#Frostlake.scalar","page":"API reference","text":"scalar(result) -> Any\n\nThe first cell of the first row, or nothing when there is none — what a single-value query (SELECT COUNT(*), SELECT CURRENT_VERSION()) is after.\n\n\n\n\n\n","title":"Frostlake.scalar"},{"category":"function","location":"api/#Frostlake.rowcount","page":"API reference","text":"Rows returned, or rows affected for a DML statement.\n\n\n\n\n\n","title":"Frostlake.rowcount"},{"category":"function","location":"api/#Frostlake.isupdate","page":"API reference","text":"Whether this result came from a DML statement rather than a query.\n\n\n\n\n\n","title":"Frostlake.isupdate"},{"category":"function","location":"api/#Frostlake.columnnames","page":"API reference","text":"The column names, in order.\n\n\n\n\n\n","title":"Frostlake.columnnames"},{"category":"function","location":"api/#Frostlake.columnindex","page":"API reference","text":"columnindex(result, name) -> Int\n\nThe position of a column, matched exactly first and case-insensitively after, or 0 when the result has no such column.\n\n\n\n\n\n","title":"Frostlake.columnindex"},{"category":"type","location":"api/#Frostlake.ZonedTimestamp","page":"API reference","text":"ZonedTimestamp\n\nA TIMESTAMP_TZ / TIMESTAMP_LTZ value: a wall clock together with the offset it was written at.\n\nJulia's standard library has no zoned type — a DateTime is a wall clock with nowhere to keep an offset — and folding the offset in would lose it, so the two halves are kept side by side. utc(z) gives the instant, which is what to compare two of these by; z.datetime gives the local reading, which is what the engine prints.\n\nz = ZonedTimestamp(DateTime(2024, 1, 15, 10, 30), Dates.Second(3600))\nutc(z)  # 2024-01-15T09:30:00\n\n\n\n\n\n","title":"Frostlake.ZonedTimestamp"},{"category":"function","location":"api/#Frostlake.utc","page":"API reference","text":"utc(z::ZonedTimestamp) -> DateTime\n\nThe instant z names, as a UTC wall clock. Two ZonedTimestamps written at different offsets are the same moment when their utc agree.\n\n\n\n\n\n","title":"Frostlake.utc"},{"category":"type","location":"api/#Frostlake.FrostlakeError","page":"API reference","text":"FrostlakeError\n\nSupertype of every failure this driver raises, so a single\n\ntry\n    execute(conn, sql)\ncatch e\n    e isa FrostlakeError || rethrow()\nend\n\ncatches the lot. The subtypes say which kind it was.\n\n\n\n\n\n","title":"Frostlake.FrostlakeError"},{"category":"type","location":"api/#Frostlake.QueryError","page":"API reference","text":"QueryError <: FrostlakeError\n\nThe engine rejected a statement: it compiled badly, referenced something that does not exist, or failed while running. message is the engine's own wording, unmodified.\n\nwarning: `statement` is sensitive\nBinding is client-side, so statement holds the SQL after parameter substitution — a bound password or card number appears in it verbatim. message and the printed form of the error carry none of it, so log those freely and treat statement as sensitive.\n\n\n\n\n\n","title":"Frostlake.QueryError"},{"category":"type","location":"api/#Frostlake.ConnectionError","page":"API reference","text":"ConnectionError <: FrostlakeError\n\nThe server could not be reached, the request died mid-flight, or what came back was not a Frostlake response at all.\n\nA statement that failed this way has an unknown fate: the request may have arrived and run before the connection broke, so it must not be blindly retried — re-running an INSERT would duplicate it.\n\nFields: message, endpoint (the URL called, when the failure names one), status (the HTTP status, or nothing when the request never completed) and cause (the underlying transport error, when there was one).\n\n\n\n\n\n","title":"Frostlake.ConnectionError"},{"category":"type","location":"api/#Frostlake.UsageError","page":"API reference","text":"UsageError <: FrostlakeError\n\nThe driver was asked for something impossible and never sent a request: a malformed DSN, a closed connection, a bind value with no SQL equivalent, an argument count that does not match the placeholders.\n\n\n\n\n\n","title":"Frostlake.UsageError"},{"category":"section","location":"development/#Development","page":"Development","text":"","title":"Development"},{"category":"section","location":"development/#Running-the-tests","page":"Development","text":"From a clone of the repository:\n\njulia --project -e 'using Pkg; Pkg.test()'\n\nThe unit tests need neither an engine nor a JVM. The integration tests start a real Frostlake server and run statements through the driver, with nothing mocked; they need Java and the engine's classpath, and skip themselves when FROSTLAKE_CLASSPATH is not set:\n\nJAVA_HOME=/path/to/jdk17 FROSTLAKE_CLASSPATH=\"<engine jar>:<dependency jars>\" \\\n    julia --project -e 'using Pkg; Pkg.test()'\n\nThe classpath is passed to java -cp unchanged, so use the platform's separator: : on Linux and macOS, ; on Windows. The engine is published on Maven Central as dev.frostlake:frostlake-db; the CI workflow shows one way to assemble its classpath with Maven.","title":"Running the tests"},{"category":"section","location":"development/#Continuous-integration","page":"Development","text":"GitHub Actions runs the whole suite, integration tests included, against engines 0.0.7 and 0.1.0 on Julia 1.10 and the latest Julia release, reports coverage to Codecov, and builds this documentation.","title":"Continuous integration"},{"category":"section","location":"statements/#Statements-and-parameters","page":"Statements and parameters","text":"execute runs a statement and returns a Result. A string holding several ;-separated statements returns one result per statement: execute_all returns all of them, and execute returns the first.","title":"Statements and parameters"},{"category":"section","location":"statements/#Statement-packs","page":"Statements and parameters","text":"From engine 0.1.0, as on Snowflake, a request carries one statement unless it says otherwise, and a longer pack is refused on its count. multi_statement_count declares how many statements one request holds, with 0 for any number:\n\nresults = execute_all(conn, \"SELECT 1; SELECT 2\"; multi_statement_count=2)\n\nThe count applies to that request only: it overrides the session's MULTI_STATEMENT_COUNT without changing it, so nothing needs restoring afterwards. Without the keyword no count is sent and the session's value decides; execute(conn, \"ALTER SESSION SET MULTI_STATEMENT_COUNT = 0\") changes that value for the whole session.","title":"Statement packs"},{"category":"section","location":"statements/#Parameters","page":"Statements and parameters","text":"Parameters are inlined into the SQL on the client, because the protocol has no server-side binding. The rules match Frostlake's JDBC driver:\n\nA ? inside a string literal, quoted identifier, $$…$$ body or comment is never a placeholder.\nWhen arguments are supplied, their number must match the placeholders exactly.\nWith no arguments at all, markers pass through to the server unchanged. There a ? is a Snowflake Scripting cursor placeholder bound by OPEN c USING (...), and :name is a Scripting variable.\n\nA statement uses either positional ? or named :name placeholders, not both. Named arguments come as a dictionary or a named tuple:\n\nexecute(conn, \"SELECT :a + :b AS total\", (a=2, b=40))\nexecute(conn, \"SELECT :a + :b AS total\", Dict(\"a\" => 2, \"b\" => 40))\n\nNames match case-insensitively, in any order. A :: cast, a := assignment and a :1 positional reference are never parameters. Neither is Snowflake's VARIANT path access: a colon directly after an expression (v:field, PARSE_JSON('…'):k, \"V\":k) reads a field, so a named marker must follow an operator, a comma or a keyword.","title":"Parameters"},{"category":"section","location":"statements/#Value-conversion","page":"Statements and parameters","text":"Julia value SQL literal\nnothing, missing NULL\nBool TRUE / FALSE\nInteger (Int, BigInt, …) the exact digits; a negative value in parentheses\nAbstractFloat the shortest round-tripping form; NaN and ±Inf as text cast to FLOAT\nAbstractString '…', with backslashes and quotes escaped\nVector{UInt8} X'hex'\nDate '…'::DATE\nTime '…'::TIME\nDateTime '…'::TIMESTAMP_NTZ\nZonedTimestamp '…'::TIMESTAMP_TZ, with its offset\nAbstractVector […], elements converted recursively\nAbstractDict {'key': …}, values converted recursively\n\nVector{UInt8} is the marker for binary data; any other vector, such as Vector{Int}, becomes an array. Snowflake has both types, and guessing from the element type would give [1, 2, 3] two meanings.\n\nA Julia DateTime has no time zone, like TIMESTAMP_NTZ; casting it to TIMESTAMP_TZ would invent an offset. Bind a ZonedTimestamp when the offset matters.","title":"Value conversion"},{"category":"section","location":"connecting/#Connecting","page":"Connecting","text":"conn = Connection(\"frostlake://localhost:18082/MY_DB?schema=PUBLIC\")\n# ...\nclose(conn)\n\nConnection contacts the server before it returns: it calls the health endpoint and applies the scope the DSN names, so a database that does not exist is reported at connect time rather than on the first query.\n\nThe do-block form closes the connection however the block exits:\n\nConnection(\"frostlake://localhost:18082\") do conn\n    scalar(execute(conn, \"SELECT CURRENT_VERSION()\"))\nend\n\nFrostlake.connect is the same function under the familiar name. It is not exported because Sockets exports connect.","title":"Connecting"},{"category":"section","location":"connecting/#DSN","page":"Connecting","text":"frostlake://host[:port][/DATABASE][?param=value&…]\n\nhttp:// and https:// are accepted too and mean the same thing. Without a port, frostlake:// uses the engine's default, 18082, and http/https use their standard ports. Write an IPv6 address in brackets: frostlake://[::1]:18082.\n\nParameter Meaning Default\nschema schema to USE on the session —\nrole role to USE on the session —\nwarehouse warehouse to USE on the session —\ntimeout how long one statement may take; 0 removes the limit 5m\nconnectTimeout how long to wait for the socket 10s\nidleLimit idle time after which the DSN's scope is re-applied; 0 disables the check 30m\ntls true to use HTTPS, like an https:// DSN false\n\nDurations are a number of seconds or carry an ms, s, m or h suffix. An unknown parameter is an error, and so is a username or password: the HTTP API has no authentication, and silently dropping a password would hide that. The parameter names match the other Frostlake drivers, so one DSN works with all of them.","title":"DSN"},{"category":"section","location":"connecting/#Keyword-arguments","page":"Connecting","text":"timeout, connect_timeout and idle_limit can also be passed to Connection, as seconds or as a fixed-length Dates.Period such as Minute(10); an explicit argument overrides the DSN. cacert and verify_certificate configure HTTPS.\n\nConnection(\"https://warehouse.internal/MY_DB\"; timeout=Minute(10), cacert=\"/etc/ca.pem\")","title":"Keyword arguments"},{"category":"section","location":"limitations/#Known-limitations","page":"Known limitations","text":"These hold for engine 0.1.0, the current release. Engine 0.0.7 adds the differences listed at the end.\n\nSessions are not released on close. The driver never ends its server session, so a closed connection's session remains until the engine's 30-minute idle expiry removes it. Opening many short-lived connections accumulates server-side sessions.\nAn expired session silently resumes at the server's default scope. The engine starts a fresh session under the expired one's id; it flags this with newSession, which the driver does not read yet. The driver re-applies the DSN's scope to a connection idle longer than idleLimit, but other session state (variables, ALTER SESSION settings) is lost. It stops re-applying once the caller issues their own USE, because the DSN no longer describes the session.\nA DateTime holds milliseconds. The engine sends timestamps with nanoseconds, but a TIMESTAMP_NTZ and the wall clock of a ZonedTimestamp are Julia DateTimes, so digits beyond the millisecond do not survive. TO_VARCHAR(ts, 'YYYY-MM-DD HH24:MI:SS.FF9') reads them. A TIME keeps its nanoseconds.\nFractional numbers arrive as Float64. A NUMBER(38,10) value beyond Float64 precision is rounded; only integral values are read exactly. Read such a column through TO_VARCHAR when the last digits matter.\nFailures carry no error code. The protocol reports only a message, with no error code or SQLSTATE, so QueryError has none.\nNo Tables.jl interface, since that would add a dependency. result.values is the data and columnnames(result) the header, which is what most table constructors take.","title":"Known limitations"},{"category":"section","location":"limitations/#Engine-0.0.7","page":"Known limitations","text":"A request may carry any number of statements.\nTimestamps arrive with millisecond precision and a TIME in whole seconds: a bound Time(1, 2, 3, 456) reads back as 01:02:03. TO_VARCHAR still reads the stored fraction.\nA TIMESTAMP_TZ arrives at offset +00:00 with the wall clock it was written with, so utc(z) is off by the original offset.\nAn empty statement is refused by the HTTP endpoint with SQL is required.\nColumns carry no length.","title":"Engine 0.0.7"},{"category":"section","location":"transactions/#Transactions-and-sessions","page":"Transactions and sessions","text":"","title":"Transactions and sessions"},{"category":"section","location":"transactions/#Transactions","page":"Transactions and sessions","text":"transaction(conn) do c\n    execute(c, \"INSERT INTO acc VALUES (1)\")\nend\n\ntransaction commits when the block returns and rolls back when it throws, rethrowing the original error; it returns the block's value. begin_transaction, commit and rollback give manual control. The engine provides read-committed isolation.\n\nA transaction belongs to the session, not to the block, so any other statement run on the same connection meanwhile joins it. Use a separate connection when that is not wanted.","title":"Transactions"},{"category":"section","location":"transactions/#Sessions-and-concurrency","page":"Transactions and sessions","text":"Each Connection holds one HTTP session. Statements on a connection are serialized, so several tasks can share it and stay on the same session; that is what carries USE, session variables and an open transaction from one statement to the next.\n\ntasks = [@async execute(conn, \"INSERT INTO t VALUES (?)\", [i]) for i in 1:8]\nforeach(wait, tasks)   # serialized, one session\n\nFor real parallelism, give each task its own connection.","title":"Sessions and concurrency"},{"category":"section","location":"results/#Results-and-types","page":"Results and types","text":"","title":"Results and types"},{"category":"section","location":"results/#Results","page":"Results and types","text":"execute returns a Result:\n\nMember Contents\nresult.columns one ColumnInfo per column: name, declared type, nullability, precision, scale, and the length of a text or binary column\nresult.values every cell, aligned with columns\nresult.updatecount rows affected by DML, or -1 for a query\nresult.counters the raw number of rows … counters behind updatecount\nrows(result) each row as a dictionary keyed by column name, built on first use\nscalar(result) the first cell of the first row\nrowcount(result) rows returned, or rows affected by DML\nisupdate(result) whether the result came from DML\ncolumnnames(result) the column names, in order\n\nA Result iterates and indexes over its rows:\n\nfor row in result\n    println(row[1], \" \", row[2])\nend\n\nresult[1]           # the first row\nresult[1, 2]        # one cell of it\nresult[1, \"NAME\"]   # the same, by column name (case-insensitive)\nmap(row -> row[1], result)\n\nrows cannot hold two columns with the same name (a self-join reports ID twice, and the later one wins); values keeps both.","title":"Results"},{"category":"section","location":"results/#Types","page":"Results and types","text":"SQL type Julia type\nintegral NUMBER, INTEGER and similar Int64, or BigInt beyond 64 bits\nfractional NUMBER, FLOAT, DOUBLE, REAL Float64\nVARCHAR and other text types String\nBOOLEAN Bool\nBINARY Vector{UInt8}\nDATE Date\nTIME Time\nTIMESTAMP, TIMESTAMP_NTZ, DATETIME DateTime\nTIMESTAMP_LTZ, TIMESTAMP_TZ ZonedTimestamp\nVARIANT, OBJECT, ARRAY String, as the engine renders it\nSQL NULL nothing\n\nA NUMBER(38,0) can exceed Int64. The driver's JSON parser keeps each number's text, so such a value arrives as an exact BigInt rather than a rounded Float64.\n\nJulia's standard library has no zoned timestamp type, so TIMESTAMP_TZ and TIMESTAMP_LTZ become a ZonedTimestamp: the wall clock (z.datetime) and its UTC offset (z.offset). Compare two of them with utc(z), which returns the instant.\n\nDate and DateTime values are read exactly as stored, without conversion to the host's time zone.","title":"Types"},{"category":"section","location":"errors/#Errors","page":"Errors","text":"Every failure is a FrostlakeError, so e isa FrostlakeError catches all of them. The subtype tells what happened:\n\nQueryError: the engine refused the statement. message is the engine's text, unmodified.\nConnectionError: no answer arrived. The host refused the connection, the socket failed, the deadline passed, or a proxy answered with something that is not a Frostlake response. The statement's outcome is unknown, so do not retry it blindly: re-running an INSERT could duplicate it.\nUsageError: the driver did not send anything. Causes include a malformed DSN, a closed connection, a value with no SQL equivalent, or an argument count that does not match the placeholders.\n\nQueryError.statement holds the rendered SQL with every parameter inlined, so a bound password appears in it verbatim. The error's printed form and message do not include it: log those, and treat statement as sensitive.","title":"Errors"}]
}
